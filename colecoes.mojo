fn main():
    # Declaração explícita de tipo
    let lista_literal_explicita: ListLiteral[Int, Int, Int] = [1, 2, 3]
    print(lista_literal_explicita)

    # Declaração implícita de tipo
    let lista_literal_implicita = [1, 2, 3]
    print(lista_literal_implicita)
